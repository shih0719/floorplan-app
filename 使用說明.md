# 平面圖設備標註 Web App — 使用說明

本文件說明怎麼把這個 App **安裝到 Android 平板並離線使用**，以及日常的**操作流程**。

---

## 一、這個 App 是做什麼的

在「平面圖」上標註「設備」（插座、燈具、開關…），並建立「設備 ↔ 迴路」的對應關係，方便現場盤點與紀錄。

核心概念：
- **設備**：放在平面圖上、有座標的點位（例如一個開關）。
- **迴路**：**從屬於某個設備**（一台設備可有多個迴路），每個迴路有自己獨立的座標，代表它**實際控制的燈具（被控負載）位置**。例如開關在左邊，它控制的燈具在右邊。
- 建立設備時，會自動在該設備下產生若干迴路（數量依設備類型設定，命名如 `3S5A-1-ch1`）。

---

## 二、安裝到 Android 平板（離線使用）

> 原理：把 **Node.js 伺服器直接跑在平板本機**，資料都存在平板上的 SQLite，因此**離線也能用**（不需網際網路）。

### 需求
- Android 平板一台。
- 有這個專案的資料夾（或在平板上取得一份）。

### 步驟 1：安裝 Termux

Termux 是 Android 上的終端機 App，能安裝 Node.js。

1. 在平板的瀏覽器開啟 **https://f-droid.org** 或 **https://termux.dev**。
2. 下載並安裝 **Termux**（建議從 F-Droid 取得，較穩定）。
   - 若 Google Play 上的 Termux 版本過舊，請改用 F-Droid 版。
3. 開啟 Termux，會看到類似命令列的畫面。

### 步驟 2：更新與安裝 Node.js

在 Termux 裡依序輸入：

```bash
pkg update -y
pkg upgrade -y
pkg install nodejs git -y
```

等它跑完。（Node.js 需 24 以上，內建 SQLite 支援。）

### 步驟 3：取得專案

你有兩種方式把專案弄到平板上：

**方式 A：直接複製資料夾**
- 用 USB 傳輸線／雲端硬碟，把整個專案資料夾（含 `server`、`client`、`package.json` 等）複製到平板的內部儲存空間，例如放到 `/sdcard/Download/floorplan-app`。
- 在 Termux 取得存取權限並切換到該資料夾：
  ```bash
  termux-setup-storage
  cd /sdcard/Download/floorplan-app
  ```

**方式 B：用 git 取得（專案已放在 GitHub）**
```bash
git clone https://github.com/shih0719/floorplan-app
cd floorplan-app
```

### 步驟 4：安裝相依套件

```bash
npm install
npm run install:all
npm run build
```

- `npm run build` 會把前端打包好，讓後端直接提供整個 App（單一 port）。

### 步驟 5：啟動

```bash
npm run dev:server
```

看到 `Server running at http://localhost:3001` 就表示成功。

### 步驟 6：開啟 App

在平板瀏覽器（建議 Chrome）開啟：

```
http://localhost:3001
```

就是完整 App。

### 步驟 7：加到主畫面（變成 App 圖示、全螢幕、離線秒開）

1. 開啟 `http://localhost:3001`。
2. Chrome 網址列右邊「⋯」→「**加到主畫面**」。
3. 主畫面會出現「平面圖設備」圖示，點開全螢幕使用，前端已快取、離線也能開。

---

## 三、Windows 平板（可選）

如果平板是 Windows：
1. 安裝 Node.js（https://nodejs.org，選 LTS）。
2. 把專案資料夾放到本機。
3. 開「命令提示字元」或「PowerShell」：
   ```bash
   cd 專案路徑
   npm install
   npm run install:all
   npm run build
   npm run dev:server
   ```
4. 瀏覽器開 `http://localhost:3001`。

> **iPad（iOS）**：目前無法直接跑 Node.js，本方案不適用。

---

## 四、日常操作流程

### 1. 新增平面圖
- 進到首頁 →「＋ 新增平面圖」→ 填名稱、樓層、區域，上傳平面圖圖片 → 儲存。

### 2. 標註設備
- 點平面圖開啟檢視（**設備圖層**）。
- **點平面圖**上要放設備的位置 → 彈出表單填名稱／類型／備註（可輸入數量一次建多台）→ 儲存。
- 同類型、同座標的設備會自動合併成一個 icon，右上角顯示數量；點 icon 可編輯／新增／刪除。

### 3. 建立迴路
- 建立設備時，會**自動在該設備下產生迴路**（數量依設備類型，命名如 `3S5A-1-ch1`）。
- 也可點頂部「迴路管理」手動新增／編輯／刪除迴路。

### 4. 放置迴路到負載位置
- 切到「**迴路**」圖層。
- 左側「放置迴路」面板會依設備列出迴路與狀態（未放置／已放置）。
- 點某個「未放置」迴路 → 再**點平面圖**，把它放到燈具（被控負載）位置。
- 放完會**自動跳到下一個未放置迴路**，可連續作業。
- 點已放置的迴路再放置，會先詢問是否「移動」。

### 5. 縮放／平移平面圖
- 滾輪縮放、雙指捏合縮放、拖曳平移；右上角有「＋ − ↺」與縮放百分比。

### 6. 查看／編輯設備與迴路
- 點平面圖上的設備 icon → 編輯或刪除設備（刪除時會提醒連帶刪除其迴路）。
- 「迴路標籤」勾選可顯示／隱藏迴路標記。

### 7. 匯出／匯入（備份與換機）
- **匯出全部**：首頁右上「匯出全部」→ 下載一個 `.json` 檔（含平面圖圖片與所有資料）。
- **匯出單一**：某張平面圖卡片上的「匯出」。
- **匯入**：首頁「匯入」→ 選取先前匯出的 `.json` → 還原。

---

## 五、常見問題

**Q：離線時能編輯嗎？**
A：可以。資料都在平板本機 SQLite，離線也能正常新增／編輯／刪除。

**Q：換平板或要備份？**
A：用「匯出全部」存成 JSON，再在新裝置「匯入」即可。圖片與資料都會一起還原。

**Q：Termux 每次重開機要重新啟動伺服器？**
A：是的，需要重新執行 `npm run dev:server`。若想開機自動啟動，可查 Termux 的「boot 套件」（`pkg install termux-services` 搭配 `sv-enable`，需自行設定）。

**Q：別台裝置想連到這台平板看？**
A：在啟動伺服器後，別的裝置用平板的區域網路 IP 開啟，例如 `http://192.168.x.x:3001`（平板 IP 可在 Termux 輸入 `ifconfig` 查詢）。跨裝置連線時建議固定在 WiFi。

---

*（本說明對應專案 README，詳細技術細節見 README.md。）*
